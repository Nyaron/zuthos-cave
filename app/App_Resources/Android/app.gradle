// Add your native dependencies here:

android {
  defaultConfig {
    generatedDensities = []
    applicationId = "fr.cedricdollinger.zythoscellar"
  }
  aaptOptions {
    additionalParameters "--no-version-vectors"
  }
}
