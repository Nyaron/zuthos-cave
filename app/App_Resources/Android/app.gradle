// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "fr.cedricdollinger.zuthoscave"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
